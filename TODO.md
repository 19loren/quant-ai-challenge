# Correções pendentes

Lista de ações levantada na revisão de código de 2026-08-16, em ordem de execução.

## Prioridade 1 — desbloqueia o pipeline

- [x] Corrigir indentação em `src/data.py:26-35` — `for line in file:` está fora do bloco `with`, arquivo é iterado já fechado.
- [x] Resolver `src/parse_b3.py` — **removido**. O bloco morto (linhas 1–92) nunca executava; o script ativo (linhas 93–163) escrevia no mesmo `data/processed/prices.parquet` que `data.py`, mas sem os filtros de mercado/tipo de ativo nem a padronização que `data.py` já faz. Conferido em `notebooks/02_build_fundamentals.ipynb` que só `prices.parquet` é consumido — os arquivos por ano em `data/processed/b3_prices/` não são referenciados em nenhum outro lugar. `data.py` passa a ser o único dono do arquivo. Arquivo está versionado no git (commit `c1c679c`), recuperável com `git checkout -- src/parse_b3.py` se necessário.

## Prioridade 2 — cobertura de dados

- [x] Adicionar fallback `_ind_` em `src/fundamentals.py:57-64` quando não houver `_con_` para uma empresa. Testado com dados reais: BPP resolveu 8 empresas via individual em 2024 e 9 em 2025 (DRE não precisou de fallback nos dois anos).
- [ ] Avaliar ampliar `DFP_YEARS` em `src/fundamentals.py:11` além de 2024–2025 — **bloqueado**: só existem os diretórios `data/raw/cvm/dfp/2024` e `2025` no repositório, não há dados de outros anos para incluir.
- [x] *(descoberto durante o teste)* `print()` com `→` (U+2192) quebrava com `UnicodeEncodeError` no console Windows (cp1252) em `data.py`, `fundamentals.py` e `validation.py`, sempre antes do `save_*()` rodar — nenhuma das três pipelines conseguia terminar e salvar o parquet nesta máquina. Trocado por `->` (ASCII) nos 6 pontos.

## Prioridade 3 — manutenibilidade

- [x] Remover o sort duplicado em `src/universe.py:380-396`. Testado com dados reais: pipeline salva `universe.parquet` normalmente (501 linhas, 5 períodos).
- [x] Criar `requirements.txt` a partir do `.venv` atual (`pip freeze`, 17 pacotes).
- [x] Criar `main.py` na raiz chamando as etapas em ordem, parando no primeiro erro. *(Atualizado na verificação final: agora chama as 8 etapas — ver seção "Verificação final" abaixo, já rodado de ponta a ponta com dado real.)*
- [x] (Opcional) Mover `PORTFOLIOS` de `src/universe.py:13-49` para config externa (`config/ibrx_portfolios.csv` — fora de `data/raw/`, que é git-ignored) e trocar páginas fixas do PDF (`[6, 7]`) por busca no texto do documento inteiro. Testado com dados reais: mesmo resultado de antes (501 linhas, 5 períodos, 100 tickers extraídos do PDF).

## Achado extra — .gitignore em UTF-16

`.gitignore` estava salvo em UTF-16LE. O Git não interpreta esse encoding, então nenhuma regra estava valendo — `data/raw/` (4+ GB), `data/processed/b3_prices/`, `notebooks/outputs/` e `__pycache__/` apareciam como não-rastreados em vez de ignorados, com risco real de irem parar num `git add -A`. Reescrito em UTF-8 puro (mesmo conteúdo), confirmado com `git status` que as pastas somem da lista de untracked.

## Ampliação de dados (2026-08-16, sessão 3)

- [x] `src/download_cvm.py` criado — baixa DFP direto do portal oficial da CVM (`dados.cvm.gov.br/dados/CIA_ABERTA/DOC/DFP/DADOS/dfp_cia_aberta_{ano}.zip`, histórico desde 2010, sem scraping). Baixados 2019–2023; `DFP_YEARS` em `fundamentals.py` ampliado para 2019–2025. Testado: `fundamentals.parquet` foi de 1.872 para 6.502 linhas (576 empresas, 2018–2025).
- [x] `data/processed/prices.parquet` regenerado com o pipeline completo (era pendência da sessão anterior) — 639.906 linhas, 685 ativos, 2019-01-02 a 2025-12-30. Rodar o ano inteiro levou ~1,4s (muito mais rápido do que eu tinha estimado antes).
- [x] IBOV (benchmark) — não precisou de scraper: o índice já vem no COTAHIST como ticker `IBOV11`, só é descartado pelo filtro de `security_type` de `data.py` (que só aceita ON/PN/PNA/PNB/UNT). Separado num passe só, sem reler os arquivos: `data/processed/ibov.parquet` (137 linhas, 2019–2025, ~monthly).
- [x] **Achado durante a validação**: 1 linha em `prices.parquet` tinha `close = 0` — `BGIP4` em 2021-05-18, pregão de negócio único onde a B3 só preencheu o preço médio na fonte. Decisão: excluído no parser (`data.py`, `zero_close_count`), já que fechamento zero não é preço de referência utilizável. Testado: 639.906 → 639.905 linhas.
- [x] **Achado durante a validação (2)**: 3 linhas duplicadas em `fundamentals.parquet` — empresas que reapresentaram o mesmo exercício (`DT_FIM_EXERC`) duas vezes no mesmo ano-calendário da CVM, com `DT_REFER` diferentes (ex.: Construtora Tenda, exercício 2022 reportado em 2023-03 e de novo em 2023-12). Só apareceu ao ampliar `DFP_YEARS`. Decisão: mantida a apresentação mais recente por `DT_REFER` (`keep_latest_filing_per_period` em `fundamentals.py`, mesmo critério que `prepare_metadata` já usa). Testado: `python -m src.validation` passa as 3 validações (preços, universo, fundamentos) de ponta a ponta.
- [x] **Nota à parte**: quando um script quebra com exceção, o traceback não aparece no terminal Windows deste ambiente (mesma raiz do bug de encoding cp1252 já corrigido, mas desta vez no próprio interpretador) — só o exit code 1 sem mensagem. Para depurar, rodar capturando a exceção manualmente (`try/except` + `traceback.print_exc()` escrevendo em arquivo UTF-8) em vez de confiar no traceback padrão no console.
## Extração de src/model.py e src/features.py (2026-08-17)

Corrigindo os desvios encontrados na avaliação da estratégia vs. o enunciado (momentum ✓, risco ✓, mas EBITDA/alavancagem fora do score e overlay de regime inexistente).

- [x] **Bug grave achado e corrigido em `fundamentals.py`**: `EQUITY_ACCOUNT = "2.07.01"` só é patrimônio líquido de verdade pra ~2% das empresas (instituições financeiras); pras outras ~96%, a conta correta é `2.03` — só que `2.03` também é reaproveitada como "Provisões" pra ~3,6% das empresas. Corrigido com `extract_account()` aceitando lista de códigos candidatos + validação por texto de `DS_CONTA` (regex `patrim[oô]nio\s+l[ií]quido`). Resultado: `equity` foi de 360 para **9.739 linhas** (quase empatado com `net_income`, como devia ser desde o início). Também troquei `NET_INCOME_ACCOUNT` de `3.11.01` (atribuído ao controlador) pra `3.11` (consolidado total), pra ficar consistente com a base de patrimônio líquido usada (também consolidado total).
- [x] **`src/features.py` criado** — extrai receita (`3.01`), EBIT (`3.05`), dívida curto/longo prazo (`2.01.04`/`2.02.01`), caixa (BPA `1.01`) e D&A. D&A não tem código de conta confiável entre empresas (achei de `6.01.01.01` a `6.01.01.19` pro mesmo conceito) — extraído por regex no texto (`deprecia|amortiza`), somando todas as linhas que batem por empresa/período. Calcula `margem_ebitda`, `margem_liquida`, `roe`, `divida_liquida/ebitda`, `quality_score`, `leverage_score`. Testado com dados reais: 6.289 linhas, 566 empresas, cobertura de 99% em quality_score e 76% em leverage_score (menor porque bancos/seguradoras não têm EBITDA no sentido tradicional — ficam de fora do fator de alavancagem por design, não por erro).
  - **Limitação conhecida**: o regex de D&A às vezes pega linhas erradas que também contêm "depreciação" no texto (ex.: "Créditos de PIS/COFINS sobre depreciação"), gerando valores levemente errados pra um subconjunto de empresas. Não é possível resolver isso sem uma lista curada por empresa — é uma aproximação, não uma extração perfeita.
- [x] **`src/model.py` criado** — momentum 12-1, vol 60 pregões, e o **overlay de regime que não existia** (Ibovespa vs. média móvel, pesos diferentes por regime). Testado e confirmado que identifica corretamente o crash de mar/2020 como "estresse" até jul/2020.
  - **Limitação conhecida e importante**: o enunciado pede MM200 (200 *pregões*), mas o COTAHIST só registra o IBOV ~1x/mês até 2024 (quase diário só a partir de dez/2025) — não dá pra calcular uma MM200 diária de verdade com esse dado. Implementei como janela de tempo corrido (~290 dias, não por contagem de linhas, já que a densidade do dado muda demais ao longo do tempo) — é a melhor aproximação possível com os dados que existem hoje, mas não é literalmente "200 pregões".
  - A alavancagem agora **entra de fato no score final** (bug do notebook onde `leverage_score` era calculado e nunca usado).
- [x] **`src/ticker_mapping.py` criado** — substitui as ~40 células de fuzzy matching manual do notebook (que só resolviam 40/124 tickers, e tinham um bug real: `assert len(universo_final) == 40` sugeria que a maioria simplesmente não tinha match e ficava fora). Abordagem: normalização de nome + match exato, depois substring com trava de tamanho mínimo (achei e corrigi um bug próprio aqui — "R/HOLDINGS S.A." normalizava pra só "R" e virava substring de tudo, contaminando 11 tickers com o mesmo CD_CVM errado), depois um dicionário de 28 overrides manuais **verificados linha a linha contra o cadastro real da CVM** (não de memória — cheguei a montar um dicionário de 24 códigos de cabeça antes de perceber o risco e descartar tudo). Resultado: **110/124 tickers (89%) mapeados com evidência real**; os 14 restantes (ASAI3, AUAU3, BBAS3, CBAV3, CCRO3, CMIN3, ODPV3, ORVR3, PCAR3, PETZ3, PRIO3, RECV3, SRNA3, TAEE11) ficam sem correspondência — não achei registro CVM inequívoco pra eles, incluindo o caso estranho do Banco do Brasil (BBAS3) não aparecer em nenhum ano do cadastro de metadados baixado.
- [x] **`src/backtester.py` criado** — rebalance mensal, retorno líquido de custo de transação (10bps por perna, proporcional ao turnover), comparação com IBOV, métricas (CAGR, vol, Sharpe, drawdown). Testado de ponta a ponta com dado real: 82 observações mensais, 2019-02 a 2025-11.
  - **Resultado do primeiro backtest, sem ajuste de parâmetros**: estratégia com CAGR 2,96% / Sharpe 0,25 vs. IBOV 8,01% / Sharpe 0,44 — a estratégia **subperformou** o benchmark no período. Reportado como saiu, sem tunar pesos/parâmetros pra melhorar o número (isso seria overfitting no próprio backtest).
- [x] **Pipeline completo roda em `src/` agora**: `universe.py` → `data.py` → `fundamentals.py` → `validation.py` → `features.py` → `ticker_mapping.py` → `model.py` → `backtester.py`. `main.py` atualizado com as 8 etapas.
- [x] **Notebooks removidos** — a lógica útil já estava extraída (e corrigida) em `src/`; o notebook 02 tinha ficado desatualizado em relação aos bugs já corrigidos (EBIT vs. EBITDA, alavancagem fora do score, overlay de regime ausente). `notebooks/02_build_fundamentals.ipynb` tinha 6.276 linhas nunca commitadas — commitado como snapshot (`ace2b87`) antes de apagar, pra não perder o histórico de exploração. Gráficos (curva de patrimônio, drawdown) migraram pra `src/backtester.py`, salvos em `reports/`.

- [ ] **Carteiras históricas do IBrX100 (pré-2025) — investigado a fundo, sem solução limpa encontrada.**
  - `b3.com.br/pesquisapregao/download?filelist=IR{AAMMDD}.zip` → XML `BVBG.087.01`: **descartado**. Decodifiquei o schema — é um boletim diário de *nível* de índice (open/high/low/close/oscilação para os 37 índices da B3, incluindo IBOV e IBXX), um valor agregado por dia. Não tem composição por ativo/peso, então não serve pra isso.
  - `sistemaswebb3-listados.b3.com.br/indexProxy/indexCall/GetPortfolioDay/`: existe, aceita parâmetros em base64 (idioma, página, índice, segmento), mas pelo nome do endpoint (`...Day`) tudo indica que só serve a carteira do dia corrente, não uma data histórica arbitrária — não testado de ponta a ponta, mas não parece ser a rota certa.
  - **Caminho que resta**: os boletins trimestrais em PDF/xlsx (o mesmo tipo de arquivo que já está em `data/raw/ibrx100/` pros 5 períodos atuais) parecem ser a única fonte confiável para períodos anteriores a 2025 — não achei um portal de bulk-download oficial equivalente ao da CVM para isso. Provavelmente exige coletar manualmente arquivo por arquivo no site do B3, um por trimestre.

## Verificação final antes da publicação (2026-08-17)

- [x] **Pipeline rodado do zero de ponta a ponta**: apaguei todos os `.parquet` processados e conteúdo de `reports/`, rodei `python main.py` (as 8 etapas) num processo só. Terminou sem erro e reproduziu exatamente os mesmos números de todos os testes incrementais anteriores (preços: 639.905 linhas; universo: 501; fundamentos: 19.477; backtest: CAGR 2,96% vs. IBOV 8,01%). Confirma que o pipeline é determinístico e não depende de estado deixado por execuções anteriores.
- [x] **Checagem de segredos/credenciais**: busca por padrões de API key/senha/token/chave privada em `src/`, e por arquivos `.env`/`.pem`/`.key`/credenciais na raiz. Nada encontrado.
- [x] **`README.md` corrigido** — descrevia uma estrutura que não existia mais (`src/data_loader.py`, `tests/`, `config.yaml`, `git-lfs`) e um título cortado ("Itaú Asset" sem "Management"). Reescrevi a árvore de diretórios pra bater com o repositório real, removi a menção a git-lfs (não está configurado — sem `.gitattributes`, `git lfs ls-files` vazio) e adicionei uma seção "Como rodar".
- [x] Removidos arquivos de debug temporários criados durante as sessões anteriores (`out.txt`, `err.txt`, `acct_check*.txt`, `unmatched_search*.txt`, `dfc_check.txt`, `pipeline_full_run.txt`) que tinham ficado soltos na raiz do repositório.
